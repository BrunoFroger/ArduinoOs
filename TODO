Liste des actions a mener :

- bug a corriger :
    - pb sur le break d'un for qui ne s'execute pas s'il n'y a pas dinstruction Serial.print() a l'interieur

- nouvelles fonctionnalites a creer :
    - cde d'edition de fichier en ligne de commande
    - cde export pour stocker une variable de script dans les variables globales
    - acces aux variables globales depuis un script ; recherche du nom de varaible dans les variables globales si on la trouve pas dans les variables locales
    - gestion de fichiers :
        - prise en compte de * pour la gestion des fichiers
        - prise en compte du ? pour la gestion des fichiers
        - commande ls :
            - gerer parametres :
                - recurssivite -r
                - afffichage simple ou complet -a
        - commande cd :
            - gerer le cd .. 
            - gerer parametres :
                - recurssivite -r

- travaux en cours :
    - gestion de fichiers :
        - deplacement de fichier
    - parentheses :
        - mettre a jour le README
        - mettre a jour l'aide

- fonctionnalite terminées a tester :
    - parentheses :
        - tester la priorite des operations
        - tester plusieurs cas d'imbrication de parentheses 
    - fonction de log avec nombre de parametres multiples
    - gestion de fichiers :
        - suppression de fichier
        - creation de repertoire
        - copie de fichier

- fonctionnalites terminees :
    - mettre fonction d'aide dans un fichier specifique

- bug corriges :
    - pwd ne fonctionne pas